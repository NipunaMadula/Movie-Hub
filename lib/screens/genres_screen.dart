import 'package:flutter/material.dart';

class GenresScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Genres')),
      body: Center(child: Text('Browse by genres (placeholder)')),
    );
  }
}
